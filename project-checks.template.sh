#!/bin/sh
# Project-owned CI hook. Add the repository's real lint, test, and build commands here.
# Keep this file small and fail on the first unsuccessful command.
set -eu

# Examples (delete these comments when configuring the project):
# npm ci
# npm run lint
# npm test

echo "No project-specific CI commands configured in scripts/ci/project-checks.sh."
